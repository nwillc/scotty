# This is a properties file template to be used with SCoTTy

# {#? company env=prod #}
{#= host.devbox1.type #}.{#= host.devbox1.instance #}.env={#= host.devbox1.env #}

{#
 output.println("# Hosts");
 Type type = database.get("host");
 for (Instance instance : type.getInstances().values()) {
    output.println(instance.getContext().get("env") + ".host." + instance.getContext().get("instance") + ".address=" + instance.getContext().get("address"));
 }
#}
# End

