# {#t application,environment,datasource #} {#c env,host #} {# context.put("app=argosub"); #}
# ARGO SUB PROPERTIES FILE
#
argosub.environment={#? name type=environment #}
argosub.user.name={#? username #}
argosub.user.password={#? password #}
argosub.domain={#? domain type=environment #}
argosub.instance.id=PSUB-{#? instance #}
argosub.sync.urls={#? syncUrls #}
argosub.proxying=true
argosub.remoteLogging.activate={#? remoteLogging #}
argopub.remoteLogging.db.host={#? host type=datasource,database=remoteLogging #}
argopub.remoteLogging.db.port={#? port type=datasource,database=remoteLogging #}

