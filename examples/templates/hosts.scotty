# {#t host #}
# Hostfile
#
{#
	Context c = new Context("type=host");
	Set set = database.query(c, "hostname");
	for (String hostname : set ) {
		c.put("hostname", hostname);
		Instance host = database.query(c).get(0);
		output.println(hostname + "\t" + host.get("ip_address"));	
	}
#}

